version: '3.8'
services:
  payments:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RAZORPAY_KEY_ID
      - RAZORPAY_KEY_SECRET
      - RAZORPAY_WEBHOOK_SECRET
      - ADMIN_USER
      - ADMIN_PASS
    volumes:
      - ./data:/usr/src/app/data
      - ./logs:/usr/src/app/logs
